import { SignUp } from "@clerk/nextjs";

const SignUpPage = () => {
    return (
        <main className="flex-center h-screen">
            <SignUp />
        </main>
    );
};

export default SignUpPage;
